{
  "hooks": {
    "PreToolUse": [
      {
        "tools": ["Write", "Edit"],
        "type": "prompt",
        "prompt": "If the file path is inside the plugins/ directory, check that: (1) the plugin folder name starts with 'clr-', (2) any 'name' field in plugin.json or SKILL.md/agent frontmatter starts with 'clr-'. If either check fails, BLOCK with a message about the clr- prefix requirement. If the file is not in plugins/, ALLOW."
      }
    ]
  }
}
